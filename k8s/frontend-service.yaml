apiVersion: v1
kind: Service

metadata:
  name: talentsphere-frontend

spec:
  selector:
    app: talentsphere-frontend

  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080

  type: NodePort
